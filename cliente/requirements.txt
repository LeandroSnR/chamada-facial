# Cliente — roda no PC da sala de aula (máquina fraca)
# Só captura e envia frames, sem processamento de IA local.
# Instalação: pip install -r requirements.txt

opencv-python==4.10.0.84
requests==2.32.3
python-dotenv==1.0.1
